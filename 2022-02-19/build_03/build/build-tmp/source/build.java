/* autogenerated by Processing revision 1278 on 2022-02-19 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.text.SimpleDateFormat;
import java.util.Date;
import processing.pdf.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class build extends PApplet {




String  folderName;
int[] palette = {0xFFED0EBD, 0xFFEF28B3, 0xFFF142AA, 0xFFF35CA0, 0xFFF57696, 0xFFF78F8D, 0xFFF9A983, 0xFFFBC379, 0xFFFDDD70, 0xFFFFF766};
float d, seg, b, e, n;
int rand = 1;
int density = 10;
int l = 10;
int s = 45;
 public void setup() {
	/* size commented out by preprocessor */;
	folderName 	= new SimpleDateFormat("yyyyMMddHHmmss").format(new Date());
}
 public void draw() {
	background(palette[0]);
	noFill();
	strokeCap(SQUARE);
	strokeWeight(width / l / 2);
//--------------------------------------------
	translate(width / 2, width / 2);
	for (int j = l / 3; j < l; ++j) {
		for (int i = 0; i < s; ++i) {
			d = j * (width / l);
			d = d - (width / l / 2);
			seg = TWO_PI / s;
			b = i * seg;
			e = b + seg;
			n = noise(j, i, rand) * palette.length-1;
			stroke(palette[1+(int)n]);
			arc(0, 0, d, d, b, e);
		}
	}
//--------------------------------------------
}
 public void mouseReleased() {
	String fileName = new SimpleDateFormat("yyyyMMddHHmmss'.pdf'").format(new Date());
	beginRecord(PDF, "../exports/" + folderName + "/" + fileName);
	rand++;
	// background(palette[0]);
}
 public void mousePressed() {
	endRecord();
}
 public void keyPressed() {
	if (key == 'r') {
		rand++;
	}
	if (key == 'x') {
		endRecord();
		exit();
	}
}


  public void settings() { size(800, 800); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "build" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
