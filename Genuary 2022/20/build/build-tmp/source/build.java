/* autogenerated by Processing revision 1276 on 2022-01-20 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class build extends PApplet {

String  pathDATA 	= "../../../0_data/";
int[] palette  	= {0xFF0160B6,0xFF03A08F,0xFFFFDD5E,0xFFFFA051,0xFFFF3C1B};
int  	bg 			= palette[1];
int  	fg 			= palette[1];
PFont   font;
float 	n;
float 	off = 0.0f;
 public void setup() {
	/* size commented out by preprocessor */;
	font = createFont(pathDATA + "fonts/ubuntu.ttf", 30);
	textAlign(CENTER, CENTER);
	textFont(font);
}
 public void draw() {
	background(bg);
	dotGrid(10);
	off += .005f;
	float vidLength = 15;
	float fps       = 30;
	saveFrame("../exports/frames/###.png"); if (frameCount == vidLength*fps) exit();
}
 public void dotGrid(int count) {
	int cell = width / count;
	int area = count * (height / cell);
	translate(cell, cell);
	for (int i = 0; i < area; i++) {
		int x = (i % count) * cell;
		int y = (i / count) * cell;
		n = noise(x, y, off);
		float n_ = n * 126;
		int cn = Math.round(n_);
		String character = str(cn);
		if (x < width - cell && y < height - cell) {
			pushMatrix();
			translate(x, y);
			rotate(map(n, 0, 1, 0, TWO_PI));
			fill(palette[2+(int)(n * 2)]);
			c(0, 0, character);
			popMatrix();
		}
	}
}
 public void c(float x, float y, String s) {
	textSize(10 + n * 100);
	pushMatrix();
	translate(x, y);
	text(s, 0, 0);
	popMatrix();
	pushMatrix();
	translate(x, y);
	rotate(TWO_PI / 3);
	text(s, 0, 0);
	popMatrix();
	pushMatrix();
	translate(x, y);
	rotate((TWO_PI / 3) * 2);
	text(s, 0, 0);
	popMatrix();
}


  public void settings() { size(1080, 1080); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "build" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
