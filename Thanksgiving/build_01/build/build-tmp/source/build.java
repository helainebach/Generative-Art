/* autogenerated by Processing revision 1276 on 2021-11-26 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class build extends PApplet {

int[] palette = {0xFF432009,0xFF512D15,0xFFE4A83B,0xFF75AD2D,0xFF2DAD91,0xFF2B4CA6,0xFF622BA6};
 public void setup() {
	/* size commented out by preprocessor */;
}
 public void draw() {
	background(0xFF152715);
	// int count = 3;
	// int area = count * count;
	// int cell = width / count;
	// for (int n = 0; n < area; n++) {
	// 	int x = (n % count) * cell;
	// 	int y = (n / count) * cell;
	// 	turkey(x+cell/2, y+cell/2, cell/5);
	// }
	turkey(mouseX,mouseY,100);
}
 public void turkey(float x, float y, float s) {
	float mY = map(y, 0, height, 9, 20);
	float mX = map(x, 0, width, mY, mY * 2);
	stroke(0);
	strokeWeight(5);
	line(x - s / 5, y, 			x - s / 5, y + s * .75f);
	line(x - s / 5, y + s * .6f, x - s / 5 + s / 10, y + s * .7f);
	line(x - s / 5, y + s * .6f, x - s / 5 - s / 10, y + s * .7f);
	line(x + s / 5, y, 			x + s / 5, y + s * .75f);
	line(x + s / 5, y + s * .6f, x + s / 5 + s / 10, y + s * .7f);
	line(x + s / 5, y + s * .6f, x + s / 5 - s / 10, y + s * .7f);
	noStroke();
	ellipseMode(CORNER);
	for (int i = 0; i < mY; i++) {
		fill(palette[2 + i % (palette.length - 2)]);
		pushMatrix();
		translate(x, y);
		rotate(PI + ((PI / mX) * i));
		ellipse(- s / 6, 0, s / 3, s * 2);
		popMatrix();
		pushMatrix();
		translate(x, y);
		rotate(PI + (PI / mX) * -i);
		ellipse(- s / 6, 0, s / 3, s * 2);
		popMatrix();
	}
	ellipseMode(CENTER);
	fill(palette[0]);
	circle(x, y, s);
	fill(palette[1]);
	circle(x, y - s / 2, s / 2);
	fill(0);
	circle(x - s / 10, y - s / 2, s / 15);
	circle(x + s / 10, y - s / 2, s / 15);
	translate(0, s / 20);
	fill(palette[2]);
	triangle(x - s / 20, y - s / 2, x + s / 20, y - s / 2, x, y - s / 2 + s / 10);
}


  public void settings() { size(600, 600); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "build" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
