/* autogenerated by Processing revision 1276 on 2021-11-26 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class build extends PApplet {

int[] palette = {0xFF75AD2D,0xFF2DAD91,0xFF2B4CA6,0xFF622BA6};
float m = 20;
 public void setup() {
	/* size commented out by preprocessor */;
}
 public void draw() {
	background(0xFF152715);
	pushMatrix();
	translate(width / 2, height / 2);
	float r = mouseX * mouseY;
	rotate(map(r, 0, width * height, 0, TWO_PI));
	flower(0, 0, map(mouseY, 0, height, 20, 50));
	popMatrix();
}
 public void flower(float x, float y, float s) {
	noStroke();
	ellipseMode(CORNER);
	for (int i = 0; i < m; i++) {
		fill(palette[i % (palette.length)]);
		pushMatrix();
		translate(x, y);
		rotate((TWO_PI / m) * i);
		ellipse(- s / 4, 0, s / 2, s * 2);
		popMatrix();
	}
	ellipseMode(CENTER);
	fill(0xFFE4A83B);
	circle(x, y, s);
}
 public void mousePressed() {
	m--;
}


  public void settings() { size(600, 600); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "build" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
